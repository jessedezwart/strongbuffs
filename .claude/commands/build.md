Build the Strong Buffs plugin JAR.

Run:
```
./gradlew shadowJar
```

On success, report the output JAR location (typically `build/libs/strongbuffs-<version>-all.jar`).
On failure, show the full error output and identify the root cause.
Do not attempt to fix errors automatically — report them clearly.
