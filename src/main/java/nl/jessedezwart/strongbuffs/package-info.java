/**
 * Plugin bootstrap and persistence entry points for Strong Buffs.
 *
 * <p>This package contains the RuneLite plugin lifecycle wiring, config entry point, and
 * persistence adapter that stores rule definitions as JSON. Runtime state, panel editing state,
 * and rendered overlays live in dedicated subpackages so persisted definitions remain pure data.</p>
 */
package nl.jessedezwart.strongbuffs;
