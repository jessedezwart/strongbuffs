/**
 * State and orchestration layer behind the Swing panel.
 *
 * <p>These classes coordinate persisted rules, draft editing, validation, and unsaved-change
 * handling so the view layer stays focused on rendering and event forwarding.</p>
 */
package nl.jessedezwart.strongbuffs.panel.state;
