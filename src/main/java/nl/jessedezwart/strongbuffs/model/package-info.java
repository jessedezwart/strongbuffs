/**
 * Persisted rule, condition, action, and editor metadata types.
 *
 * <p>Classes in {@code model} describe what the user configured, not what the game client is doing
 * right now. They are safe to serialize, validate, copy, and edit in Swing without touching the
 * RuneLite API.</p>
 */
package nl.jessedezwart.strongbuffs.model;
