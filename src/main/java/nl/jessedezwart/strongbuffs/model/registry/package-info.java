/**
 * Catalog of approved persisted condition and action types.
 *
 * <p>The catalog is the source of truth for editor creation and polymorphic persistence. Type ids
 * resolve back to metadata and factories here instead of being scattered across the codebase.</p>
 */
package nl.jessedezwart.strongbuffs.model.registry;
